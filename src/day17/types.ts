export type Point = {
  x: number;
  y: number;
};

export type Probe = Point;

export type TargetArea = {
  minX: number;
  maxX: number;
  minY: number;
  maxY: number;
};

export type Velocity = Point;
